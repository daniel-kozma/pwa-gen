const generateManifest = ({ id, name, short_name, description, orientation, display, scope, start_url, theme_color, background_color, icons }) => ({
    id,
    name,
    short_name,
    description,
    orientation,
    display,
    scope,
    start_url,
    theme_color,
    background_color,
    icons
});
export default generateManifest;
